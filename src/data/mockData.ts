
import { Novel } from '../types/novel';

// Simple search function for novels - will be replaced by backend search
export const searchNovels = (query: string): Novel[] => {
  // Return empty array since we're using backend data now
  // This file exists only for compatibility until search is fully integrated
  return [];
};
