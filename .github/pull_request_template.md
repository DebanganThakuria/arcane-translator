## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How it was tested

<!-- For a source change, say which site and which novel you tested against. -->

## Checklist

- [ ] `go build ./... && go vet ./...` pass, and `gofumpt -l .` prints nothing
- [ ] `npm run check` passes in `web/`
- [ ] No keys, ARNs or account identifiers added to the tree
- [ ] No generated files, binaries or database contents committed
